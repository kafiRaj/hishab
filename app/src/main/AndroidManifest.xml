<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.amarhisab">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <!-- Declare the MainActivity with android:exported -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" />
        <activity
            android:name=".UserActivity"
            android:exported="true" />

        <activity
            android:name=".DashboardActivity"
            android:exported="true" />

        <activity
            android:name=".IncomeSectionActivity"
            android:exported="true" />

        <activity
            android:name=".IncomeSectionGridView"
            android:exported="true" />

        <activity
            android:name=".IncomeExpenseActivity"
            android:exported="true" />

        <activity
            android:name=".IncomeExpenseGridView"
            android:exported="true" />
        <activity
            android:name=".BudgetAddActivity"
            android:exported="true" />

        <activity
            android:name=".BudgetGridView"
            android:exported="true" />

        <activity
            android:name=".ReportActivity"
            android:exported="true" />

    </application>

</manifest>
